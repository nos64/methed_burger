export interface IProduct {
  id: string;
  title: string;
  price: number;
  weight: number;
  calories: number;
  description: string;
  image: string;
  ingredients: string[];
  category: string;
}
